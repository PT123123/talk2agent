# 候选开源项目清单（本地优先 · 全双工 · C++ 为主）

> 结论：**没有单一仓库能开箱即用满足全部条件**。最接近的按三档归类，多数是 Python，C++ 只有组件级和框架级。

---

## 第一档：架构最接近（全双工 + Agent + 工具）

| 项目 | 语言 | 许可 | 命中点 | 短板 |
|---|---|---|---|---|
| [TEN-framework](https://github.com/TEN-framework/TEN-framework) + [TEN-Agent](https://github.com/TEN-framework/TEN-Agent) | C++ 核心，扩展支持 C++/Go/Python | Apache-2.0 **with additional restrictions** | 官方定位就是 realtime multimodal voice agent；自带 **TEN VAD** + **TEN Turn Detection**（文本型轮次判定）+ **MCP 集成** + TMAN 可视化编排 + ESP32 落地 | 默认依赖 Agora RTC / Azure STT-TTS / OpenAI key，本地化要自己换扩展；许可条款商用前必读 |
| [kyutai-labs/moshi](https://github.com/kyutai-labs/moshi) | PyTorch / Rust / MLX | 开源权重 | 原生**全双工 multi-stream**，真实建模打断、附和、重叠语音，"没有轮流" | 7B 语音到语音，工具/推理能力弱于文本 LLM；难接搜索 |
| [kyutai-labs/unmute](https://github.com/kyutai-labs/unmute) | Python（服务端） | 开源 | 级联式 STT→LLM→TTS；**semantic VAD = 不打断你**；LLM 可任意替换；支持 function calling；总延迟 <1s | 需 CUDA ≥16GB、仅 Linux/WSL；组件是 Python |
| [kyutai-labs/delayed-streams-modeling](https://github.com/kyutai-labs/delayed-streams-modeling) | Python | 开源 | Unmute 的 STT/TTS 单独可用 | 同上 |

**可抄的核心**：Unmute 的 semantic VAD 时序（"说完了才响应，不中途打断"）；Moshi 的 multi-stream 全双工范式。

---

## 第二档：能立刻跑起来、验证全链路（多为 Python，但接口设计可直接借鉴）

| 项目 | 语言 | 命中点 | 短板 |
|---|---|---|---|
| [huggingface/speech-to-speech](https://github.com/huggingface/speech-to-speech) | Python | 四种模式：`local`（直连麦克风）、`realtime`（复刻 OpenAI Realtime API）、`websocket`、`socket`；可把 LLM 指向本地 **llama-server**；STT 可选 parakeet-tdt / whisper / paraformer，TTS 可选 Qwen3-TTS / Kokoro；**自带 web search 工具**（`--enable_web_search`，有 tool call 轮次上限） | Python；turn-taking 较简单，无强打断语义 |
| [ShayneP/local-voice-ai](https://github.com/ShayneP/local-voice-ai) | Python + Docker Compose | LiveKit 实时媒体 + Whisper(VoxBox) + llama.cpp + Kokoro + RAG，一键 `./test.sh` | ~300 star，轻量 demo，非全双工 |
| [mudler/LocalAI](https://github.com/mudler/LocalAI) | **C/C++ 引擎群** + Go 外壳 | 自研 18 个引擎：`parakeet.cpp`(ASR，CPU 比 whisper.cpp turbo 快约 27x)、**LocalVQE（AEC+降噪+去混响，16ms 延迟）**、`moss-tts`/`magpie-tts`/`voxtral-tts.c`、`ced.cpp`(声音事件)、`voice-detect.cpp`(声纹/年龄/情绪)、`local-store`(本地向量库) + realtime API | 是"服务端"不是"编排层"；Go 侧外壳重 |
| [mudler/LocalAGI](https://github.com/mudler/LocalAGI) | Go | Agent 平台：MCP、知识库、LocalRecall 长期记忆、定时任务、OpenAI Responses 兼容；已嵌入 LocalAI v4.0 核心 | 非语音实时链路；无全双工 |
| [LyaQanYi/OpenAIRealtimeTransport](https://github.com/LyaQanYi/OpenAIRealtimeTransport) | Python | 本地复刻 OpenAI Realtime API 协议（URL/事件/音频编码），内置 Silero VAD + 能量回退，自由麦模式，浏览器 WebUI | Python；用于"接口对齐"参考 |

**可抄的核心**：speech-to-speech 的 `--mode local` / Realtime 协议双形态（= 你要的"预留在线接口"）；LocalAI 的 AEC+VAD 组合方案。

---

## 第三档：打断与状态机的工程参考（端-云分离）

| 项目 | 语言 | 命中点 |
|---|---|---|
| [78/xiaozhi-esp32](https://github.com/78/xiaozhi-esp32) | C（ESP-IDF，MIT） | 设备端**全双工状态机**（监听/说话/空闲）+ AEC + **MCP 工具**；Opus 60ms 帧上传；157 个板型变体 |
| [xiaozhi-esp32-server](https://github.com/xinnan-tech/xiaozhi-esp32-server)（及 wantt/bhswallow 分支） | Python | 服务端 VAD(Silero) → ASR → LLM → TTS 的 provider 抽象 + 流式 TTS + 打断处理；配置驱动可换任意 ASR/TTS/LLM |
| [pipecat](https://github.com/pipecat-ai/pipecat) / [livekit/agents](https://github.com/livekit/agents) | Python | 打断处理、turn detection、上下文管理；**Smart Turn v3.2 的 ONNX 权重可直接拿进 C++ 用**（BSD-2，权重+训练代码全开源） |

**可抄的核心**：xiaozhi 的 provider 抽象 + 打断时序（中文资料最全）；Pipecat Smart Turn 的 ONNX 端点模型。

---

## 组件级 C++ 库（拼装用，全部可静态链接）

- **音频前后端**：`k2-fsa/sherpa-onnx`（ASR/VAD/TTS/降噪/声纹/语种，官方 C++ API，CUDA + RKNN/QNN/Ascend/Axera NPU）
- **AEC**：`libwebrtc` AEC3（BSD）；备选 `speexdsp`
- **VAD**：`TEN-framework/ten-vad`（C 动态库 + ONNX，16kHz，hop 160/256）；`Silero VAD v5`
- **LLM**：`ggml-org/llama.cpp`（CUDA/Vulkan/ROCm，GBNF 结构化输出 + tool calling + `/reranking` + `/v1/embeddings`）
- **MCP**：`mcpp-project/mcpp`（C++20，MIT）、`GopherSecurity/gopher-mcp`（C++）
- **传输**：`libdatachannel`（WebRTC）/ `uWebSockets`（本地 WS）

## 小型 C++ 组合脚手架（star 少，但结构可看）

- `novik133/jarvis`：C++/QML 桌面助手，whisper.cpp + llama.cpp + Piper
- `wongfei/UEHMI`：UE5 插件，sherpa-onnx + llama.cpp + Piper 离线对话
- `mawwalker/stt-server`：C++ WebSocket 流式 ASR 服务（sherpa-onnx + SenseVoice + VAD）

## 建议的三条路

1. **路线 A（省事）**：以 **TEN framework** 为骨架，用 C++ 写扩展替换掉 Agora/Azure 依赖，接 sherpa-onnx + llama.cpp。代价：受其许可与框架约束。
2. **路线 B（推荐验证）**：先跑 **speech-to-speech / Unmute** 把"响不响、像不像人"验证出来，同时自研 C++ 编排层，抄它们的 turn-taking 时序，Smart Turn ONNX 直接搬。
3. **路线 C（全自研）**：C++ 编排层 + LocalAI（当本地服务端/引擎供给）+ sherpa-onnx + llama.cpp。

> 三条路共同的"必须自己写"部分：**AEC 参考信号对齐、双讲下的打断判定级联取消、EOU 语义端点**——这三块在开源界几乎没有现成的 C++ 实现。
